# 核心依赖
PyQt6>=6.5.0
aiohttp>=3.10.11
qrcode>=7.4.0
pillow>=10.0.0  # qrcode需要
tqdm>=4.65.0
qasync>=0.27.1
regex>=2023.0.0
# AICU API需要的curl_cffi（用于绕过Cloudflare）
curl-cffi>=0.5.0

PyQt6-Qt6>=6.4.0
requests>=2.28.0